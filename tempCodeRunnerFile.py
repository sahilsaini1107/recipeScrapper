        # li_elements = soup.find('div', id='content').div.div.ul.find_all('li')
        # for li in li_elements:
        #         additems.append( li.text )
        #         print(li.text)
        # ingred = soup.find('div', id='content').div.div.find_all('h2')
        # for h in ingred:
        #     additems.append(h.text)
        #     additems.append(h.find_next_sibling().text)
        # print(additems)
        # csv_file = "output.csv"
        # with open(csv_file, mode='w', newline='', encoding='utf-8') as file:
        #     writer = csv.writer(file)
        #     writer.writerow(["List Item Text"]) 
        #     writer.writerows(additems)